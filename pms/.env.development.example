## PMS dev env example
## Copy to `.env.development.local` and adjust as needed.
##
## A안(권장): 로컬 PMS가 배포 API를 사용하도록 설정
## - PMS dev 서버는 `/api/**` 요청을 아래 타겟으로 프록시합니다.
VITE_API_PROXY_TARGET=https://crossmanagern.web.app
VITE_API_PROXY_TARGET=http://localhost:3000

## (선택) apiClient baseURL을 직접 덮어쓰고 싶을 때만 사용
## - 일반적으로는 `/api` + 프록시 조합을 권장합니다.
# VITE_API_BASE_URL=https://crossmanager.web.app/api

