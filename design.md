# 화면설계서 작성 형식

화면설계서는 모든 모듈에서 이 형식으로 만든다. 기준 사례는 SMS 화면설계서(2026-09-14)다.

- 설계서 뷰어: `docs/sms/screens/sms-screen-viewer.html`
- 편집 캔버스: `docs/sms/screens/sms-screen-spec.html`
- 생성 도구: `docs/sms/screens/tools/` (5장)

---

## 1. 산출물

| 산출물 | 파일 | 용도 |
|---|---|---|
| **설계서 뷰어** | `docs/<모듈>/screens/<모듈>-screen-viewer.html` | 기본 산출물. 상단 그룹 메뉴로 화면을 넘겨 보고 내려받는다 (3장) |
| 편집 캔버스 | `docs/<모듈>/screens/<모듈>-screen-spec.html` | 여러 화면을 한 판에 펼쳐 놓고 직접 고치는 설계판. Artifact로 게시 |
| 화면 원본 | `docs/<모듈>/screens/src/*.dc.html` · `canvas.json` · `notes.json` | 캔버스와 뷰어를 다시 만들 때 쓰는 원본 |
| 화면별 HTML | `docs/<모듈>/screens/html/<모듈>_<ID>_<화면명>.html` | 화면 한 장씩 여는 파일 |
| 화면 이미지 | `docs/<모듈>/screens/shots/<ID>.png` | 운영가이드·보고서에 넣는 그림 (2배 해상도) |
| 운영가이드 | `docs/<모듈>/<모듈>_운영가이드_<대상>_v<버전>.docx` | 요청이 있을 때만. 화면 이미지와 번호 설명을 그대로 쓴다 |

- 모든 HTML은 외부 파일 없이 혼자 열린다. 로고는 파일 안에 넣고(base64), 글꼴만 Google Fonts에서 받는다.
- 설계서 뷰어와 편집 캔버스는 Artifact로 게시하고 링크를 전달한다.

## 2. 화면 구성 규칙

### 2.1 화면 ID와 파일명

| ID | 뜻 |
|---|---|
| `S00` | 표지 · 화면 목록 |
| `S01`~ | PC 화면 |
| `M01`~ | 현장 모바일 화면 |

- 파일명은 `<모듈>_<ID>_<화면명>.html` 로 짓는다. 화면명의 공백과 가운뎃점(·)은 하이픈으로 바꾼다.
  - 예: `SMS_S05_위험성평가-작성-수정.html`
- 편집 캔버스 파일명은 영문 소문자만 쓴다(게시 규칙). 예: `sms-screen-spec.html`

### 2.2 화면마다 적는 정보

| 항목 | 내용 | 예 |
|---|---|---|
| 화면명 | 사용자가 부르는 이름 | 위험성평가 작성·수정 |
| 경로 | 앱 URL | `/sms/ra/new · /sms/ra/:id/edit` |
| 구분 | **신규**(새 화면) / **개선**(기존 화면 수정) | 개선 |
| 단계 | 개발계획의 Phase | Phase 2 |
| 요약 | 이 화면이 하는 일 한두 문장 | |
| 번호 설명 | 화면 위 주황 번호 ①②③과 짝을 이루는 설명 | |
| 크기 | PC 1440px 폭 · 모바일 390px 폭. 높이는 내용에 맞춘다 | 1440 × 1815 |

### 2.3 표지 (S00)

- 설계서명, 버전, 작성일, 근거 문서, 화면 수(PC n · 모바일 n)
- 화면 목록 표: ID · 화면 · 경로 · 구분 · 단계
- 모든 화면에 적용되는 공통 규칙 (예: 현장은 상단에서 한 번만 고른다, 등급 표기 기준)
- **"화면의 현장명·인명·수치는 예시입니다"** 고지

### 2.4 그리는 방식

- **실제 앱 스타일을 그대로 쓴다.** 그 모듈의 CSS에서 색·글꼴·간격·모서리·표·배지 값을 옮긴다. 새로 만드는 요소도 기존 스타일을 따른다.
- **번호 표식**: 설명할 요소 옆에 주황 원을 붙인다(`#fb923c`, 지름 22px, 글자 `#1a0f05`, 굵게). 번호는 화면 오른쪽 설명 목록과 같은 순서로 매긴다.
- **예시 데이터**: 현장·사람·업체는 누가 봐도 예시인 이름을 쓴다(예: `[P-2026-003]`, 협력사A, `user@cross.example`). 실제 기록처럼 보이는 값은 쓰지 않는다.
- **모바일 화면**: 사이드바 없이 한 손으로 쓰는 전용 형태로 그린다. 폭 390px, 버튼 높이 44px 이상. 가짜 상태바는 그리지 않는다.
- **정적 화면**으로 그린다. 눌러 보는 시제품이 필요하면 따로 요청을 받는다.

### 2.5 SMS 스타일 값 (다른 모듈은 그 모듈 값을 쓴다)

| 항목 | 값 |
|---|---|
| 바탕 | `#0b1324` |
| 글자 | 본문 `#e8ecf7` · 보조 `#9fb2cc` · 흐림 `#64748b` |
| 선 | `rgba(255,255,255,0.08)` · 입력칸 `0.12` |
| 주 색 | 버튼 `#3b82f6`(누름 `#2563eb`) · 탭 밑줄 `#63a4ff` |
| 글꼴 | `'Segoe UI', 'Noto Sans KR'` |
| 틀 | 상단바 76px · 좌측 사이드바 232px(선택 메뉴 바탕 `#475569`) · 본문 여백 24px |
| 카드 | 모서리 14px · 안쪽 여백 1.2rem · 패널 1.25rem |
| 표 | 글자 0.85rem, 머리글 아래 2px 선 (기준정보 표와 같다) |
| 위험성 등급 | A 20~25 빨강 · B 15~19 노랑 · C 10~14 파랑 · D 5~9 하늘 · E 1~4 회색 |

## 3. 설계서 뷰어 형식

설계서의 기본 산출물이다. 한 파일 안에서 모든 화면을 오가며, **목차 페이지를 따로 만들지 않는다.**

```
┌──────────────────────────────────────────────────────────────────────┐
│ [로고] 설계서명·버전 │ 개요 · 대시보드 · 위험요인 · … · 현장 모바일 │ [뷰어 HTML 받기] [이 화면만] │  ← 1줄: 업무 그룹
├──────────────────────────────────────────────────────────────────────┤
│ [S04 위험성평가 목록] [S05 위험성평가 작성·수정] [S06 …]              │  ← 2줄: 그룹 안 화면
├───────────────────────────────────────────────┬──────────────────────┤
│ 1440 × 1815 px  [맞춤|100%]  72%   5 / 16 ‹ › │ S05 · 개선 · Phase 2 │
│                                               │ 위험성평가 작성·수정  │
│        (점 격자 바탕 위에 화면 미리보기)        │ /sms/ra/new          │
│                                               │ 요약                  │
│                                               │ ① … ② … ③ …          │
│                                               │ 받는 파일명 · 크기     │
└───────────────────────────────────────────────┴──────────────────────┘
```

### 3.1 상단 메뉴

- **1줄 업무 그룹**: 파란 밑줄 탭에 그룹 이름과 화면 수를 표시한다.
  - 순서: **개요(표지)** → 업무 그룹들 → **현장 모바일(마지막)**
  - 모바일 화면은 업무 그룹에 섞지 않고 「현장 모바일」 한 그룹에 모은다.
- **2줄 화면**: 고른 그룹의 화면을 칩으로 나열한다. ID는 고정폭 글꼴로 쓰고, 모바일 화면에는 "모바일" 표시를 붙인다.
- SMS 그룹 예: 개요 · 대시보드 · 위험요인 · 위험성평가 · TBM · 점검·순찰 · 지적사항 · 기준정보 · 현장 모바일

### 3.2 가운데 미리보기

- 옅은 점 격자 바탕 위에 화면을 올린다. PC는 얇은 테두리와 그림자, 모바일은 둥근 기기 틀.
- 도구줄: 화면 크기(px) · [맞춤 | 100%] · 현재 배율 · 위치(n / 전체) · 이전·다음
- ← → 키로 이전·다음 화면. 마지막으로 본 화면과 배율을 기억한다.

### 3.3 오른쪽 설명 (360px)

- ID · 구분 칩(신규 초록 / 개선 파랑) · 단계 · 현장 모바일 표시
- 화면명, 경로(고정폭 글꼴), 요약
- 번호 설명: 주황 원 번호 + 설명. 화면 위 번호와 1:1로 맞는다.
- [이 화면만]으로 받을 때의 파일명과 크기

### 3.4 내려받기

| 버튼 | 받는 것 |
|---|---|
| **[뷰어 HTML 받기]** (주 버튼) | **지금 보는 뷰어를 그대로** 파일 하나로. 받을 때 보던 화면·배율로 열리고, 받은 파일에서도 같은 버튼이 동작한다 |
| [이 화면만] | 선택한 화면 한 장만 담은 HTML |

- ZIP 묶음과 목차 페이지(`index.html`)는 만들지 않는다. 파일 사이를 오가게 되기 때문이다.
- 게시된 페이지에서는 플랫폼 저장 창을 쓰고, 파일로 열었을 때는 브라우저 내려받기를 쓴다.

### 3.5 화면 폭별 동작

- 1000px 이하: 오른쪽 설명이 미리보기 아래로 내려간다.
- 720px 이하: 그룹 탭이 한 줄 전체를 쓰고 가로로 넘긴다.

## 4. 작업 순서

1. **범위 확정**: 근거 문서(개발계획 등)에서 화면 목록을 뽑는다. 화면마다 ID · 경로 · 구분 · 단계를 정한다.
2. **스타일 확인**: 해당 모듈의 CSS와 기존 화면 코드에서 값을 옮긴다. 파일은 읽기만 한다.
3. **화면 작성**: 공통 틀(상단바·사이드바)은 한 번만 만들고 화면별 본문만 작성한다. 번호 표식과 번호 설명을 함께 쓴다.
4. **높이 맞춤**: 화면별 실제 내용 높이를 재서 잘리지 않게 한다.
5. **이미지**: 화면 이미지를 2배 해상도로 만든다(운영가이드용).
6. **게시**: 편집 캔버스와 설계서 뷰어를 Artifact로 올리고 링크를 전달한다.
7. **운영가이드**: 요청이 있을 때만 만든다. 표지에 "개발 완료 후 화면 기준 여부"를 밝힌다.

### 지킬 것

- 설계서 작업은 코드를 바꾸지 않는다. 새 파일만 `docs/<모듈>/` 아래에 만든다.
- 이미지 캡처나 확인에 브라우저를 쓰면 CLAUDE.md의 브라우저 테스트 금지와 관계되므로 사용자에게 알린다. 앱에는 접속하지 않고 설계 파일만 연다.
- 화면 이미지 폴더는 용량이 크다(SMS 16장 기준 12MB). 커밋 여부는 사용자에게 확인한다.
- 개발 전에 그린 설계서는 "최종 단계 기준"임을 표지에 적는다. 개발이 끝나면 실제 화면으로 갱신한다.

## 5. 생성 도구 (`docs/sms/screens/tools/`)

SMS 설계서를 만든 스크립트다. 다른 모듈은 이 폴더를 복사해 화면 파일(`screens_*.mjs`)과 그룹 구성만 바꿔 쓴다. 경로는 모두 스크립트 위치 기준이라 어느 PC·어느 폴더에서 실행해도 결과가 같다.

| 파일 | 하는 일 |
|---|---|
| `shell.mjs` | 공통 틀(상단바·사이드바·모바일 틀), 스타일 값, 아이콘, 번호 표식·등급 배지 도우미 |
| `screens_a.mjs` · `screens_b.mjs` · `screens_m.mjs` | 화면별 본문과 설명(화면명·경로·구분·단계·요약·번호 설명) |
| `build.mjs` | 화면 원본 `src/*.dc.html` · `canvas.json` · `notes.json` 생성 |
| `measure.py` | 화면별 실제 내용 높이 측정 → `heights.json` |
| `capture.mjs` | 화면 이미지 `shots/*.png` 생성 |
| `viewer.mjs` · `viewer.template.html` | 설계서 뷰어와 화면별 HTML 생성. 업무 그룹 구성은 `viewer.mjs`의 `GROUPS` |
| `guide.py` | 운영가이드 워드 생성 |
| `heights.json` | 측정한 화면 높이 |

### 실행 순서

```bash
node docs/sms/screens/tools/build.mjs          # 1. 화면 원본
node docs/sms/screens/tools/capture.mjs        # 2. 화면 이미지 (ID를 주면 그 화면만: capture.mjs S05 S09)
node docs/sms/screens/tools/viewer.mjs         # 3. 설계서 뷰어 + 화면별 HTML
python docs/sms/screens/tools/guide.py         # 4. 운영가이드 (요청이 있을 때만)
```

화면 높이를 다시 재야 할 때(화면 내용을 크게 바꾼 경우)는 1번 전에 다음을 실행한다.

```bash
MEASURE=1 node docs/sms/screens/tools/build.mjs && python docs/sms/screens/tools/measure.py
# PowerShell 에서는:  $env:MEASURE='1'; node ...build.mjs; Remove-Item Env:MEASURE; python ...measure.py
```

- 측정값이 3000을 넘는 화면은 직접 고친다. 화면 전체를 덮는 창이 있는 화면(S11)은 960, 표지는 900이다. `heights.json`에서 수정한다.
- 편집 캔버스(`*-screen-spec.html`)는 스크립트로 만들지 않는다. Claude Code의 `/design` 기능으로 `src/` 파일을 묶어 만들고 게시한다.
- 필요한 것: Node 20, Python 3 + `python-docx` · `Pillow`, Microsoft Edge(이미지·높이 측정용).
- `capture.mjs` · `measure.py`는 화면 없는 Edge를 쓴다. 4장 "지킬 것"의 브라우저 사용 고지 대상이다.
- 실행 중 생기는 작업 폴더(`edge-profile/`, `measure/`, `guide_img/`)는 `.gitignore`로 제외했다.
